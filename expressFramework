-- express is a web framwork
-- npm i express
-- npm uninstall url

const http = require("http");
const express = require("express");
const app = express();

app.get("/" , (req,res)=>{
    return res.send("Hello from home page")
})
app.get("/about" , (req,res)=>{
    return res.send("Hello from about page")
})
app.listen(8000, ()=>console.log("Server Started"))




-- version
-- 4.18.2
-- 1st part -4(Major Release/Breaking Update)
-- 2nd part - 18(Recommended Bug fix{Security fix})
-- 3rd part - 2(Minor fixes)(optional updates)
-- npm install express@4.17.2  //our define verion like we want to install some specific version

